# Automatic sync evaluation report

No human-ground-truth evaluation has been run yet. Add at least three private case files and ground-truth files, then run `evaluate.py`. Generated audio paths, case data, ground truth, and raw results are gitignored.
